import { Bell, Mail, ShieldCheck, UserRound } from "lucide-react";
import { PageHeader } from "@/components/app-shell/page-header";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
export default function SettingsPage() { return <div className="page-container"><PageHeader eyebrow="Preferences" title="Settings" description="Frontend placeholders for profile, recipe inbox, and notification preferences." /><div className="grid max-w-3xl gap-5"><Card><CardContent><div className="flex gap-3"><UserRound className="text-primary" /><div><h2 className="section-heading">Profile</h2><p className="section-copy">How your name appears in PerfectPlate.</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm font-bold">Name<Input defaultValue="Meg Malan" /></label><label className="grid gap-2 text-sm font-bold">Email<Input type="email" defaultValue="meg@example.com" /></label></div><Button className="mt-5">Save profile</Button></CardContent></Card><Card><CardContent><div className="flex gap-3"><Mail className="text-primary" /><div><h2 className="section-heading">Recipe inbox</h2><p className="section-copy">Your assigned intake address will appear here after backend setup.</p></div></div><div className="mt-5 rounded-lg bg-surface-subtle p-4 text-sm font-bold">recipes@inbox.perfectplate.local <span className="ml-2 font-normal text-muted-foreground">Demo only</span></div></CardContent></Card><Card><CardContent><div className="flex gap-3"><Bell className="text-primary" /><div><h2 className="section-heading">Notifications</h2><p className="section-copy">Choose when imports and subscriptions should get your attention.</p></div></div>{["A recipe import is ready", "An import needs review", "A subscription needs attention"].map((label) => <label className="mt-4 flex items-center justify-between gap-4 border-t border-border pt-4 text-sm font-bold" key={label}>{label}<input type="checkbox" defaultChecked /></label>)}</CardContent></Card><p className="flex gap-2 text-xs text-muted-foreground"><ShieldCheck size={16} />Settings are local visual controls until the backend is connected.</p></div></div>; }
