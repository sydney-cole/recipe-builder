import { Bell, Mail, ShieldCheck, UserRound } from "lucide-react";
import { PageHeader } from "@/components/app-shell/page-header";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
export default function SettingsPage() { return <div className="page-container"><PageHeader eyebrow="Preferences" title="Settings" description="Profile and notification controls are previews." /><div className="grid max-w-3xl gap-5"><Card><CardContent><div className="flex gap-3"><UserRound className="text-primary" /><div><h2 className="section-heading">Profile preview</h2><p className="section-copy">Profile editing is not connected yet.</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm font-bold">Name<Input disabled placeholder="Profile name" /></label><label className="grid gap-2 text-sm font-bold">Email<Input disabled type="email" placeholder="Account email" /></label></div><Button className="mt-5" disabled>Save profile</Button></CardContent></Card><Card><CardContent><div className="flex gap-3"><Mail className="text-primary" /><div><h2 className="section-heading">Shared recipe inbox</h2><p className="section-copy">One configured inbox serves the whole site. Its live address and your verified-email eligibility appear on the Imports page.</p></div></div></CardContent></Card><Card><CardContent><div className="flex gap-3"><Bell className="text-primary" /><div><h2 className="section-heading">Notification preview</h2><p className="section-copy">Notification preferences are not connected yet.</p></div></div>{["A recipe import is ready", "An import needs review", "A subscription needs attention"].map((label) => <label className="mt-4 flex items-center justify-between gap-4 border-t border-border pt-4 text-sm font-bold" key={label}>{label}<input type="checkbox" disabled /></label>)}</CardContent></Card><p className="flex gap-2 text-xs text-muted-foreground"><ShieldCheck size={16} />Preview controls cannot change account data.</p></div></div>; }
